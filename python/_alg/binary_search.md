## range index
```
1. >= x // lower bound，最左边的(第一个)大于等于x的数。
x = 2, arr = [1 3 3 4], 找到 i = 1，arr[i] = arr[1] = 3 第一个3
2. > x      <=>     >= x+1
3. < x      <=>     (>= x)-1
4. <= x     <=>     (> x)-1
```
